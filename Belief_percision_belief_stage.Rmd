---
title: "Regressions - Percision in beliefs as a function of subjects' average emotions"
output:
  html_document:
    number_sections: yes
    toc: yes
    toc_depth: 3
    toc_float: yes
---

$Y_{i,t} = C_{i} + \beta_1Neutrality_{i,t} + \beta_2Happiness_{i,t} + \beta_3Sadness_{i,t} + \beta_4Anger_{i,t} + \beta_5Surprise_{i,t} + \beta_6Fear_{i,t} + \beta_7Disgust_{i,t}$
(Fixed effects regression model)

$Y_{i,t} = |\frac{Belief_{i,t,k=0}-Price_{t}}{Price_{t}}|$  (only 1 period percision).

Each emotion is averaged over 10 s', during belief stage in period _t_, per each subject.

This calculation is considering the belief stage's emotion, while the depending value is shown only in the record stage. 
Meaning:  "How does the subject emotion during the belief stage contribute to the subject precision."

# All sessions – combined {.tabset}

## All subjects

```{r, echo= FALSE, message=FALSE, warning=FALSE}
library(plm)
library(jtools)
library(huxtable)
library(tidyverse)

mydata <- read.csv(file="C:/Users/Liran/Google Drive/Study/PhD/Face_Reader/The_Experiment/Belief_precision/Belief_precision_belief_stage.csv", header=TRUE, sep=",")

market1 = subset(mydata, period<=15)
market2 = subset(mydata, period>15)
#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))

```

## Traders only

```{r, echo= FALSE, message=FALSE, warning=FALSE}

traders  <- subset(mydata, traders_1==1)

market1 = subset(traders, period<=15)
market2 = subset(traders, period>15)
#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))

```

## Observers only

```{r, echo= FALSE, message=FALSE, warning=FALSE}

Observers <- subset(mydata, traders_1==0)

market1 = subset(Observers, period<=15)
market2 = subset(Observers, period>15)
#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))

```

# Session 1 {.tabset}

## All subjects
```{r, echo= FALSE, message=FALSE, warning=FALSE}
market1 = subset(mydata, period<=15)
market2 = subset(mydata, period>15)
market1 = subset(market1, s_num == "S1")
market2 = subset(market2, s_num == "S1")

#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))
```



## Traders only
```{r, echo= FALSE, message=FALSE, warning=FALSE}

traders  <- subset(mydata, traders_1==1)

market1 = subset(traders, period<=15)
market2 = subset(traders, period>15)
market1 = subset(market1, s_num == "S1")
market2 = subset(market2, s_num == "S1")
#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))

```


## Observers only
```{r, echo= FALSE, message=FALSE, warning=FALSE}

Observers <- subset(mydata, traders_1==0)

market1 = subset(Observers, period<=15)
market2 = subset(Observers, period>15)
market1 = subset(market1, s_num == "S1")
market2 = subset(market2, s_num == "S1")
#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))

```

# Session 2 {.tabset}

## All subjects
```{r, echo= FALSE, message=FALSE, warning=FALSE}
market1 = subset(mydata, period<=15)
market2 = subset(mydata, period>15)
market1 = subset(market1, s_num == "S2")
market2 = subset(market2, s_num == "S2")

#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))
```



## Traders only
```{r, echo= FALSE, message=FALSE, warning=FALSE}

traders  <- subset(mydata, traders_1==1)

market1 = subset(traders, period<=15)
market2 = subset(traders, period>15)
market1 = subset(market1, s_num == "S2")
market2 = subset(market2, s_num == "S2")
#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))

```


## Observers only
```{r, echo= FALSE, message=FALSE, warning=FALSE}

Observers <- subset(mydata, traders_1==0)

market1 = subset(Observers, period<=15)
market2 = subset(Observers, period>15)
market1 = subset(market1, s_num == "S2")
market2 = subset(market2, s_num == "S2")
#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))

```

# Session 3 {.tabset}

## All subjects
```{r, echo= FALSE, message=FALSE, warning=FALSE}
market1 = subset(mydata, period<=15)
market2 = subset(mydata, period>15)
market1 = subset(market1, s_num == "S3")
market2 = subset(market2, s_num == "S3")

#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))
```



## Traders only
```{r, echo= FALSE, message=FALSE, warning=FALSE}

traders  <- subset(mydata, traders_1==1)

market1 = subset(traders, period<=15)
market2 = subset(traders, period>15)
market1 = subset(market1, s_num == "S3")
market2 = subset(market2, s_num == "S3")
#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))

```


## Observers only
```{r, echo= FALSE, message=FALSE, warning=FALSE}

Observers <- subset(mydata, traders_1==0)

market1 = subset(Observers, period<=15)
market2 = subset(Observers, period>15)
market1 = subset(market1, s_num == "S3")
market2 = subset(market2, s_num == "S3")
#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))

```

# Session 4 {.tabset}

## All subjects
```{r, echo= FALSE, message=FALSE, warning=FALSE}
market1 = subset(mydata, period<=15)
market2 = subset(mydata, period>15)
market1 = subset(market1, s_num == "S4")
market2 = subset(market2, s_num == "S4")

#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))
```



## Traders only
```{r, echo= FALSE, message=FALSE, warning=FALSE}

traders  <- subset(mydata, traders_1==1)

market1 = subset(traders, period<=15)
market2 = subset(traders, period>15)
market1 = subset(market1, s_num == "S4")
market2 = subset(market2, s_num == "S4")
#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))

```


## Observers only
```{r, echo= FALSE, message=FALSE, warning=FALSE}

Observers <- subset(mydata, traders_1==0)

market1 = subset(Observers, period<=15)
market2 = subset(Observers, period>15)
market1 = subset(market1, s_num == "S4")
market2 = subset(market2, s_num == "S4")
#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))

```

# Session 5 {.tabset}

## All subjects
```{r, echo= FALSE, message=FALSE, warning=FALSE}
market1 = subset(mydata, period<=15)
market2 = subset(mydata, period>15)
market1 = subset(market1, s_num == "S5")
market2 = subset(market2, s_num == "S5")

#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))
```



## Traders only
```{r, echo= FALSE, message=FALSE, warning=FALSE}

traders  <- subset(mydata, traders_1==1)

market1 = subset(traders, period<=15)
market2 = subset(traders, period>15)
market1 = subset(market1, s_num == "S5")
market2 = subset(market2, s_num == "S5")
#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))

```


## Observers only
```{r, echo= FALSE, message=FALSE, warning=FALSE}

Observers <- subset(mydata, traders_1==0)

market1 = subset(Observers, period<=15)
market2 = subset(Observers, period>15)
market1 = subset(market1, s_num == "S5")
market2 = subset(market2, s_num == "S5")
#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))

```

# Session 6 {.tabset}

## All subjects
```{r, echo= FALSE, message=FALSE, warning=FALSE}
market1 = subset(mydata, period<=15)
market2 = subset(mydata, period>15)
market1 = subset(market1, s_num == "S6")
market2 = subset(market2, s_num == "S6")

#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))
```



## Traders only
```{r, echo= FALSE, message=FALSE, warning=FALSE}

traders  <- subset(mydata, traders_1==1)

market1 = subset(traders, period<=15)
market2 = subset(traders, period>15)
market1 = subset(market1, s_num == "S6")
market2 = subset(market2, s_num == "S6")
#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))

```


## Observers only
```{r, echo= FALSE, message=FALSE, warning=FALSE}

Observers <- subset(mydata, traders_1==0)

market1 = subset(Observers, period<=15)
market2 = subset(Observers, period>15)
market1 = subset(market1, s_num == "S6")
market2 = subset(market2, s_num == "S6")
#Fix effect market 1
Fixed_effects1 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market1, index = c("id","t_k") , model="within")
#Fix effect market 2
Fixed_effects2 = plm(precision_pct ~ neutral + happy + sad + angry + surprised + scared + disgusted, data=market2, index = c("id","t_k") , model="within")

export_summs(Fixed_effects1, Fixed_effects2,scale = TRUE,digits = 3,
             model.names = c("Market1", "Market2"),statistics = c(N = "nobs",Adj_R2 = "adj.r.squared", R2 = "r.squared") , coefs =  c("Neutrality" = "neutral" , "Happiness" = "happy", "Sadness" = "sad", "Anger" = "angry","Surprise" = "surprised", "Fear" = "scared", "Disgust" = "disgusted" ),error_format = "({p.value})", stars = c(`***` = 0.01, `**` = 0.05, `*` = 0.1))

```